# Test Environment Configuration
# Copy this file to .env.test for local testing

# Test Database
# For Docker Compose test database (db-test service):
TEST_DATABASE_URL=postgresql+asyncpg://attendance_user:test_password_123@localhost:5433/attendance_tracker_test

# For CI/CD (GitHub Actions):
# TEST_DATABASE_URL=postgresql+asyncpg://attendance_user:test_password@localhost:5432/attendance_tracker_test

# Note: Tests will use this database URL from environment variable.
# Make sure the test database is running before executing tests.
